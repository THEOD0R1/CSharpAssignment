<UserControl x:Class="Presentation.WpfApp.Views.ProductEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             
            xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
            xmlns:models="clr-namespace:Infrastructure.Models;assembly=Infrastructure"
            d:DataContext="{d:DesignInstance Type=viewmodels:ProductEditViewModel}"
             
            Background="white"
             >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Height="20" Grid.Row="0" Text="{Binding PageTitle}" Margin="0 0 0 10" />

        <StackPanel Grid.Row="1">
            <Label Content="Name" />
            <TextBox Text="{Binding ProductToUpdate.Name}" Margin="0 0 0 10" />
            <Label Content="Price" />
            <TextBox Text="{Binding ProductToUpdate.Price}" Margin="0 0 0 10" />
            <Label Content="Category" />
            <TextBox Text="{Binding ProductToUpdate.Category.CategoryName}" Margin="0 0 0 10" />
            <Label Content="Maunfacture" />
            <TextBox Text="{Binding ProductToUpdate.Manufacture.ManufactureName}" Margin="0 0 0 10" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Content="Save" Margin="0 0 0 5" Command="{Binding UpdateProductCommand}"/>
            <Button Content="Cancel" Margin="0 0 0 5" Command="{Binding CancelProductUpdateCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
